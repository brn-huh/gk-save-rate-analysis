# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# 비밀/환경변수 (API 키 등 — 절대 커밋 금지)
.env
.env.local
.env.*.local
!.env.local.example

# 데이터 원본 (ouid 등 계정정보 포함 — 절대 커밋/배포 금지)
data/
*.duckdb
*.duckdb.wal

# out/ 은 공개 배포물(민감정보 0)이라 커밋한다 → Vercel 정적 배포.

# macOS
.DS_Store
